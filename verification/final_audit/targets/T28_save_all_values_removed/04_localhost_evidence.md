# T28 — Localhost evidence

**Page:** `http://localhost:8001/landuse/`
**Screenshot:** `verification/final_audit/screenshots/localhost/02_landuse.png`

Eyeball: NO "Save All Values" / "Alle Werte speichern" button on /landuse/. The page surfaces are: row-level "Benutzer (%)" inputs, info icons, and at the bottom a "Letzte Änderungen" panel with "Speicher löschen" + edit pencil + collapse caret. Top-right `Szenarien` dropdown remains the canonical save path.

**Cross-page note:** /gebaeudewarme/ DOES still show "Alle Werte speichern" button (verified in `verification/final_audit/screenshots/localhost/05_gebaeudewaerme.png`). T28's PDF source was specifically about /landuse/, so this is consistent with the literal target — but the same UX problem the PDF flagged exists on Gebäudewärme. Documented as caveat in 08.
