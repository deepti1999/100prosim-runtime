# T28 — Performance

UI removal — no perf delta. The `/api/save-all-inputs/` endpoint is left available per the plan; it has no callers from the LandUse page anymore, but other pages may. POST timings unchanged.
