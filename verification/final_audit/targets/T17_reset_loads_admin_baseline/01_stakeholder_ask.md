# T17 — Reset to baseline loads admin baseline

**PDF §2.4.2** (page 4, lines 154–156): *"…Menüpunkt 'Baseline' → 'Auf Baseline zurücksetzen' dazu nutzen, als Baseline das administrierte Basisszenario zu laden…"*

**Acceptance:** Reset uses an admin-curated baseline (not user's last capture).
