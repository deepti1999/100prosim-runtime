# T12 — Data model from external Excel file

**PDF §2.3.2** (page 3): *"Schnittstelle zur Nutzung der bestehenden Excel-Datenmodell-Dateien anstelle des integrierten Datenmodells."*

**Acceptance:** parameter data can be loaded into the app from an external Excel file (`D.xlsx` / per-region variants).
