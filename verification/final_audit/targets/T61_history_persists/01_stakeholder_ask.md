# T61 — Each modification persisted

**PDF §2.5.8** (page 12): *"In 100prosim-Excel lässt sich die Modifikation des Basis-Szenarios Schritt für Schritt protokollieren."*

**Acceptance:** Every user save creates a `ModificationHistoryEntry` with before/after values, who, what, when.
